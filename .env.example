# Optional API keys. Every key is optional; sources needing one are skipped when absent.
# Names only, never commit values.
SHODAN_API_KEY=
VIRUSTOTAL_API_KEY=
HIBP_API_KEY=
CENSYS_API_ID=
CENSYS_API_SECRET=
# Optional. Without it the MalwareBazaar panel shows "needs a key" and everything else still works.
ABUSECH_AUTH_KEY=
